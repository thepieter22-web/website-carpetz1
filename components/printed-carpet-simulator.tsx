"use client"; import { useState, useMemo } from "react"; import Link from "next/link"; import { Upload, X } from "lucide-react"; type Quality = "velvet" | "globosoft" | "pxl"; const QUALITIES: { id: Quality; label: string; pricePerM2: number; description: string }[] = [ { id: "velvet", label: "Velvet", pricePerM2: 20, description: "Zacht en luxueus, ideaal voor showrooms." }, { id: "globosoft", label: "Globosoft", pricePerM2: 40, description: "Duurzaam en slijtvast, voor intensief gebruik." }, { id: "pxl", label: "Pxl", pricePerM2: 60, description: "Scherpste fotorealistische print, premium kwaliteit." }, ]; const PRESET_WIDTHS = [1, 2]; export function PrintedCarpetSimulator() { const [quality, setQuality] = useState<Quality>("velvet"); const [width, setWidth] = useState<number>(1); const [isCustomWidth, setIsCustomWidth] = useState(false); const [length, setLength] = useState<number>(2); const [pattern, setPattern] = useState<string | null>(null); const [showContactModal, setShowContactModal] = useState(false); const selectedQuality = QUALITIES.find((q) => q.id === quality)!; const price = useMemo(() => { return width * length * selectedQuality.pricePerM2; }, [width, length, selectedQuality]); const handleCustomWidthChange = (value: string) => { const parsed = parseFloat(value.replace(",", ".")); if (isNaN(parsed)) return; if (parsed > 2) { setShowContactModal(true); return; } setWidth(parsed); }; const handlePatternUpload = (file: File) => { const reader = new FileReader(); reader.onload = () => { setPattern(reader.result as string); }; reader.readAsDataURL(file); };   const MAX_BOX = 360;
  const MIN_BOX = 60;

  const { boxWidth, boxHeight } = useMemo(() => {
    if (!width || !length) return { boxWidth: MAX_BOX, boxHeight: MAX_BOX };
    const scale = Math.min(MAX_BOX / width, MAX_BOX / length);
    return {
      boxWidth: Math.max(MIN_BOX, width * scale),
      boxHeight: Math.max(MIN_BOX, length * scale),
    };
  }, [width, length]); return ( <section className="border-t border-border bg-secondary/40"> <div className="mx-auto max-w-7xl px-6 py-16 lg:px-8"> <h2 className="font-serif text-3xl font-semibold leading-tight text-balance"> Bereken een prijsindicatie </h2> <p className="mt-3 max-w-2xl text-sm text-muted-foreground"> Kies een kwaliteit, geef je afmetingen op en upload eventueel een patroon om een idee te krijgen van het resultaat. Dit is een prijsindicatie &mdash; voor een defintieve offerte nemen we graag persoonlijk contact met je op. </p> <div className="mt-10 grid gap-10 lg:grid-cols-[1fr_380px]"> {/* Linkerkolom: configuratie */} <div className="min-w-0 space-y-8"> {/* Kwaliteit */} <div> <h3 className="text-sm font-semibold uppercase tracking-widest text-accent">Kwaliteit</h3> <div className="mt-4 grid gap-3 sm:grid-cols-3"> {QUALITIES.map((q) => ( <button key={q.id} type="button" onClick={() => setQuality(q.id)} className={`rounded-sm border p-4 text-left transition-all ${ quality === q.id ? "border-accent bg-accent/10" : "border-border hover:border-accent/50" }`} > <div className="font-semibold">{q.label}</div> <div className="mt-1 text-xs text-muted-foreground">{q.description}</div> <div className="mt-2 text-sm font-medium text-accent">&euro;{q.pricePerM2}/m&sup2;</div> </button> ))} </div> </div> {/* Breedte */} <div> <h3 className="text-sm font-semibold uppercase tracking-widest text-accent">Breedte</h3> <div className="mt-4 flex flex-wrap gap-3"> {PRESET_WIDTHS.map((w) => ( <button key={w} type="button" onClick={() => { setIsCustomWidth(false); setWidth(w); }} className={`rounded-sm border px-5 py-3 text-sm font-medium transition-all ${ !isCustomWidth && width === w ? "border-accent bg-accent/10" : "border-border hover:border-accent/50" }`} > {w}m </button> ))} <button type="button" onClick={() => setIsCustomWidth(true)} className={`rounded-sm border px-5 py-3 text-sm font-medium transition-all ${ isCustomWidth ? "border-accent bg-accent/10" : "border-border hover:border-accent/50" }`} > Eigen keuze </button> </div> {isCustomWidth && ( <div className="mt-3"> <input type="number" step={0.1} min={0.1} max={2} placeholder="Breedte in meter (max. 2m)" className="w-48 rounded-sm border border-border p-3 text-sm" onChange={(e) => handleCustomWidthChange(e.target.value)} /> </div> )} </div> {/* Lengte */} <div> <h3 className="text-sm font-semibold uppercase tracking-widest text-accent">Lengte</h3> <div className="mt-4"> <input type="number" step={0.1} min={0.1} value={length} onChange={(e) => setLength(parseFloat(e.target.value) || 0)} className="w-48 rounded-sm border border-border p-3 text-sm" /> <span className="ml-2 text-sm text-muted-foreground">meter</span> </div> </div> {/* Patroon upload */} <div> <h3 className="text-sm font-semibold uppercase tracking-widest text-accent"> Patroon (optioneel) </h3> <div className="mt-4"> {pattern ? ( <button type="button" onClick={() => setPattern(null)} className="flex items-center gap-2 rounded-sm border border-border px-4 py-3 text-sm hover:border-accent/50" > <X className="size-4" /> Patroon verwijderen </button> ) : ( <label className="flex w-fit cursor-pointer items-center gap-2 rounded-sm border border-dashed border-border px-6 py-4 text-sm text-muted-foreground hover:border-accent/50"> <Upload className="size-4" /> Upload een wederkerend patroon <input type="file" accept="image/*" className="hidden" onChange={(e) => { const file = e.target.files?.[0]; if (file) handlePatternUpload(file); }} /> </label> )} </div> </div> </div> {/* Rechterkolom: preview + prijs */} <div className="space-y-6 lg:sticky lg:top-24 lg:self-start">             <div
              className="flex items-center justify-center rounded-sm border border-border bg-secondary/30"
              style={{ height: MAX_BOX }}
            >
              <div
                className="rounded-sm border border-border bg-muted overflow-hidden"
                style={{
                  width: boxWidth,
                  height: boxHeight,
                  backgroundImage: pattern ? `url(${pattern})` : undefined,
                  backgroundRepeat: "repeat",
                  backgroundSize: "80px 80px",
                }}
              />
            </div><div className="rounded-sm border border-border bg-card p-6"> <div className="flex justify-between text-sm text-muted-foreground"> <span>Kwaliteit</span> <span className="font-medium text-foreground">{selectedQuality.label}</span> </div> <div className="mt-2 flex justify-between text-sm text-muted-foreground"> <span>Afmetingen</span> <span className="font-medium text-foreground"> {width}m &times; {length}m </span> </div> <div className="mt-4 flex items-center justify-between border-t border-border pt-4"> <span className="font-semibold">Prijsindicatie</span> <span className="text-2xl font-bold text-accent">&euro;{price.toFixed(2)}</span> </div> <p className="mt-1 text-xs text-muted-foreground">Excl. btw &mdash; richtprijs, geen definitieve offerte.</p> <Link href={`/contact?product=geprint-tapijt&kwaliteit=${selectedQuality.label}&breedte=${width}&lengte=${length}&prijsindicatie=${price.toFixed(2)}`} className="mt-6 block w-full rounded-sm bg-accent px-4 py-3 text-center text-sm font-medium text-accent-foreground hover:opacity-90 transition-opacity" > Neem contact op voor een offerte </Link> </div> </div> </div> </div> {/* Modal voor afwijkende breedte */} {showContactModal && ( <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 px-6"> <div className="max-w-sm rounded-sm bg-card p-8 text-center"> <h3 className="text-lg font-semibold">Afwijkende breedte</h3> <p className="mt-3 text-sm text-muted-foreground"> Voor breedtes boven 2 meter werken we op maat. Neem contact met ons op zodat we dit voor je kunnen bekijken. </p> <div className="mt-6 flex flex-col gap-3"> <Link href="/contact" className="rounded-sm bg-accent px-4 py-3 text-sm font-medium text-accent-foreground hover:opacity-90" > Naar contactpagina </Link> <button type="button" onClick={() => setShowContactModal(false)} className="text-sm text-muted-foreground hover:text-foreground" > Sluiten </button> </div> </div> </div> )} </section> ); }
